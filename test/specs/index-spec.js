var mocha = require('mocha');
var chai = require('chai');
var expect = chai.expect;

describe('Test', function() {
  describe('Zebeleo', function() {
    it('open', function() {
      var open = 'never';
      expect(open).to.be.equal('never');
    })
  })
});
